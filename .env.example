# FTP credentials for deploy.sh - fill in and save as .env
# NEVER commit .env or pass it on.
FTP_HOST=example.org
FTP_USER=username
FTP_PASS=password
# Target directory on the webspace (the root of the (sub)domain):
FTP_DIR=/
